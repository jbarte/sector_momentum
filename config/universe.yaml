# Scan-wide settings.
#
# The US/EU sector universes that used to live here were retired on 2026-08-05
# (see the retire-sector-cohort design doc). The scoring universe is now
# config/themes.yaml; this file keeps only the settings that are not
# cohort-specific.
price_lookback_days: 252
